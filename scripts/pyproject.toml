[project]
name = "blog-audio-pipeline"
version = "0.1.0"
description = "TTS audio-generation pipeline for igormcsouza/blog"
requires-python = ">=3.12"
dependencies = [
    "edge-tts>=7.0.0",
    "boto3>=1.34",
]

[tool.uv]
package = false
